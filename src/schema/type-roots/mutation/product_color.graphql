
type Mutation {

  addProductColor(productColor: ProductColorInput!): ResultProductColor

#   updateSize(id: ID,size: SizeInput!): ResultSize

  deleteProductColor(productId: ID!): ResultProductColor

#   blockSize(id: ID!): ResultSize

#   unBlockSize(id: ID!): ResultSize
    
}