
type Mutation {

  addSize(size: SizeInput!): ResultSize

  updateSize(id: ID,size: SizeInput!): ResultSize

  deleteSize(id: ID!): ResultSize

  blockSize(id: ID!): ResultSize

  unBlockSize(id: ID!): ResultSize
    
}