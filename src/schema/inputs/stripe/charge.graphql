input ChargeInput {
    token: String
    "Cantidad a pagar en formato string para convertirlo de entero a cero decimal"
    amount: String!
    "Resumen del pedido"
    description: String!
    "Cliente Id"
    customer: ID!
    "Divisa"
    currency: CurrencyEnum = EUR
}